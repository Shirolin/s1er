import 'dart:typed_data';

/// Non-web stub: browser canvas encoding is unavailable.
Future<Uint8List?> encodeRgbaWithBrowser({
  required Uint8List rgbaBytes,
  required int width,
  required int height,
  required String mimeType,
  double quality = 0.85,
}) async =>
    null;
